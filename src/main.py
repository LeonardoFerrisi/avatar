# from ..lib.trinity import client, fbcca, master, sfpr, platform_nodes

import brainflow

import numpy as np


result = np.arange(7.0, 16.0+1, 3.0)

result_list = result.tolist()

## 

print(str(result_list))